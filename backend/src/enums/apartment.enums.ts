export enum ListingType {
    Sale = 'sale',
    Rent = 'rent',
}

export enum ListingStatus {
    Active = 'active',
    Pending = 'pending',
    Sold = 'sold',
    Rented = 'rented',
    Expired = 'expired',
    Deleted = 'deleted',
}
